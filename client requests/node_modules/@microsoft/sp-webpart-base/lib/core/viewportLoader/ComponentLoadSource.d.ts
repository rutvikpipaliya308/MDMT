/**
 * The source which triggered the loading of a viewport aware component.
 */
declare const enum ComponentLoadSource {
    /**
     * Component is loaded as it was found in the viewport, including a threshold
     * if any is provided.
     */
    viewport = 1,
    /**
     * Component is loaded due to scrolling.
     */
    scroll = 2,
    /**
     * Component was loaded forcefully using the release() method. Component did not
     * occupy the viewport before loading.
     */
    release = 3
}
export default ComponentLoadSource;
//# sourceMappingURL=ComponentLoadSource.d.ts.map