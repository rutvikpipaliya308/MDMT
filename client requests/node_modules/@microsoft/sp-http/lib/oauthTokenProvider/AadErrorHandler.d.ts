import { IAuthenticationContextInternal } from './AdalAuthContext';
export declare class AadErrorHandler {
    /**
   * This function will fire an event if the AAD error code can be resolved with a full page redirect.
   */
    static _handleInteractionRequiredFailures(authContext: IAuthenticationContextInternal, errorMessage: string, resource: string | undefined): void;
    /**
     * Returns true if the error can be resolved with a user interaction, such as performing Multi-Factor Authentication.
     */
    static _doesErrorCodeExist(errorMessage: string, setOfErrorCodes: Set<string>): boolean;
}
//# sourceMappingURL=AadErrorHandler.d.ts.map